@model IEnumerable<GameNight.Models.User>

<h1>Players</h1>
@foreach (var item in Model)
{
    <div>
        <h2><a href="/User/Details/@item.Id">@item.Username</a></h2>
    </div>
    <div>
        <img src="@item.ProfilePic" />
    </div>
}
